name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    name: Test Job
    runs-on: ubuntu-latest
    steps:
      - name: Echo Message
        run: echo "Hello! GitHub Actions runner is working correctly at $(date)"
