# Voice Relay Worklog Archive - 2026-05-25

Archived entries from WORKLOG.md.

---

### 2026-05-25 01:24 UTC - Orchestrator (productive tick)

🚀 **Spawned 2 workers in parallel** — drained the merge-ready PR and addressed the broken-main blocker discovered last tick.

**Active Workers:**

| Conv ID | Type | Working on | Status |
|---------|------|------------|--------|
| `65d2d64` | review (merge) | [PR #331 — feat(websocket): unified session-state](https://github.com/jpshackelford/voice-relay/pull/331) | running |
| `4b9cf9a` | implementation | [Issue #333 — fix(tests): ReleaseNotes 'last week' regression](https://github.com/jpshackelford/voice-relay/issues/333) | running |

**Slot usage:** expansion 0/4 · implementation 1/1 · review 1/2

**Action taken**

1. **Spawned merge worker for PR #331** — `[Merge] PR #331 — feat(websocket): unify session-state wire message`. PR is MERGEABLE/CLEAN, all CI green, 0 unresolved review threads, no blocking labels → ready to squash. Worker briefed on the broken-main caveat (its own Client Tests pass, the `ReleaseNotes.test.tsx` regression is unrelated) and instructed to proceed with the merge.
   - 🔗 https://app.all-hands.dev/conversations/65d2d64addce4b30b6890a411c644ad3
2. **Filed Issue #333** — [`fix(tests): ReleaseNotes timestamp test fails as fixture date ages into 'last week' bucket`](https://github.com/jpshackelford/voice-relay/issues/333), labels `bug, ci-failure, ready, priority:high`. Full root-cause analysis + recommended deterministic fix (pin `mockChangelog.releases[*].deployedAt` to `Date.now() - N*60*1000`) included in the issue body, so it shipped pre-expanded with `ready` already attached.
3. **Spawned implementation worker for Issue #333** — `[Implementation] Issue #333 — fix(tests): ReleaseNotes 'last week' regression`. Worker briefed on the scope guard (test-only fix, only `client/src/components/ReleaseNotes.test.tsx`) and the WORKLOG/state-file Branch Hygiene CI gate.
   - 🔗 https://app.all-hands.dev/conversations/4b9cf9a8c12d46d58b1202751c10b834

**Why this matters**

- PR #331 has been merge-ready since 23:25 UTC yesterday; the previous tick observed it but couldn't dispatch (OH Cloud API was returning 401). API credentials are restored this tick, so the backlog drains.
- The `ReleaseNotes.test.tsx` "last week" failure has been red on `main` for 5+ consecutive CI runs and is also surfacing on every feature branch's Client Tests check (including PR #332). Fixing the fixture is a tiny, surgical, test-only change — but it's blocking signal across the entire repo.
- PR #332 (Issue #297, memory replay) remains open and red on the same broken-main test. Once #333 lands on main and PR #332 is rebased, its Client Tests should go green and it can advance to review.

**Open issues this tick (post-spawn)**

- `ready`: #295 ✱(behind PR #331), #297 ✱(behind PR #332), #333 ✱(behind impl worker just spawned)
- `on-hold`: #210, #239, #299, #300, #301, #302 — all under the S3 design freeze per AGENTS.md
- Nothing needs expansion right now.

**Next tick expectations**

- PR #331 → merged to main (closes #295).
- New PR opens against #333 with deterministic fixture fix; Client Tests should be green.
- PR #332 likely still red on Client Tests until rebased on top of #333's fix.

**`quiet_ticks` = 0** (productive tick: 2 workers spawned, 1 critical issue filed and dispatched).

_This worklog entry was generated by an AI agent (OpenHands /orchestrate) on behalf of @jpshackelford._

---
### 2026-05-25 01:24 UTC - Merge worker (PR #331)

✅ **Merged: [PR #331](https://github.com/jpshackelford/voice-relay/pull/331) — `feat(websocket): unify session-state wire message and refactor useAI as a reducer`**

- Squash commit: [`a2850f1`](https://github.com/jpshackelford/voice-relay/commit/a2850f1db570990f71bc2d7e0b1ed4a595a03ed5)
- Closed Issue: [#295](https://github.com/jpshackelford/voice-relay/issues/295) (auto-closed via `Closes #295` trailer)
- CI on the PR: 7/7 green at merge time
- Review threads: 1 total, all resolved
- Diff scope: 17 files (+1119 / −121); `client/` + `server/src/` TypeScript only
- **Migration check:** ✅ no DB schema changes (no migration files, no SQLite mutations)
- **Production:** deploy to vr.chorecraft.net will trigger automatically; wire change is additive (new `session-state` emitted alongside legacy `session-ai-status` / `ai-thinking`), safe for rolling clients

**Notable: this PR also pre-emptively fixed the `ReleaseNotes.test.tsx` relative-time flake** (broadened regex to include `last week`, `in 3 months`, etc.). The separate fix-PR for issue #333 mentioned in the merge brief may now be redundant — main's Client Tests should turn green on the next CI run.

PR description was refreshed before merge to reflect the final state (added the inline `startingSince` comment, the broadened ReleaseNotes regex, and a migration / production notes section).

_This worklog entry was generated by an AI agent (OpenHands merge worker) on behalf of @jpshackelford._

---
### 2026-05-25 01:28 UTC - Implementation Worker (Issue #333)

✅ **Completed: PR #334 - fix(tests): pin ReleaseNotes mock fixtures**

- Issue: [#333 - ReleaseNotes timestamp test fails as fixture date ages into 'last week' bucket](https://github.com/jpshackelford/voice-relay/issues/333)
- PR: [#334 - fix(tests): pin ReleaseNotes mock fixtures relative to Date.now()](https://github.com/jpshackelford/voice-relay/pull/334)
- Scope: test-only change, single file (`client/src/components/ReleaseNotes.test.tsx`)

**What was done:**
- Replaced hardcoded `deployedAt: '2026-05-17T22:49:31Z'` with `new Date(Date.now() - 5 * 60 * 1000).toISOString()` (and 15 min for the older entry), pinning fixture timestamps relative to current time.
- Set `generatedAt` to `new Date().toISOString()`.
- Derived expected absolute-format month name dynamically via `Intl.DateTimeFormat('en', { month: 'short' })` so the "May" assertion at line 286 (and its companion at line 300) works in any month/year.
- Kept the relative-format regex `/ago|yesterday|today|just now/i` tight — fixture is now bounded to the "minutes ago" bucket.

**Verification:**
- Local: `npm test -- ReleaseNotes.test.tsx` → 19/19 pass. Full client suite → 960/960 pass.
- CI on PR #334: all 7 checks green (Client Tests, Server Tests, E2E Tests, Build Client, enable-orchestrator, lint-pr-title; pr-review skipped on draft as expected).

**PR moved to ready-for-review.** Review/merge handled by next orchestrator cycle.

---
### 2026-05-25 01:35 UTC - Orchestrator (productive tick)

🚀 **Spawned merge worker** for the test-fixture fix that's already CI-green.

**Active Workers (after this tick):**

| Conv ID | Type | Working on | Status |
|---------|------|------------|--------|
| `bef3099` | review (merge) | [PR #334 — fix(tests): pin ReleaseNotes mock fixtures](https://github.com/jpshackelford/voice-relay/pull/334) | **NEW** |

**Slot usage:** expansion 0/4 · implementation 0/1 · review 1/2

**Drained from previous tick** (both finished cleanly per OH Cloud API):

| Conv ID | Type | Outcome |
|---------|------|---------|
| `4b9cf9a` | implementation | Created [PR #334](https://github.com/jpshackelford/voice-relay/pull/334) — deterministic fixture pin for ReleaseNotes test |
| `65d2d64` | review (merge) | Merged [PR #331](https://github.com/jpshackelford/voice-relay/pull/331) (squash `a2850f1`, closed #295) |

**Action taken**

- **Spawned `bef3099` — merge worker for PR #334**. PR is `MERGEABLE/CLEAN`, 9/9 checks green (Client/Server/E2E/Build Client/pr-review SUCCESS, lint-pr-title, enable-orchestrator), 0 unresolved review threads, only `github-actions` COMMENTED. Worker briefed on: (a) test-only scope (`client/src/components/ReleaseNotes.test.tsx`), (b) no DB migration needed, (c) the `docs(worklog):` commit-prefix convention for the WORKLOG entry, and (d) the Branch Hygiene CI gate requiring WORKLOG/state changes to land on `main` only.
   - 🔗 https://app.all-hands.dev/conversations/bef30999a48c4357a6dec674c0245542

**Why this tick is single-action**

PR #332 (memory replay, Issue #297) is also open but `mergeStateStatus=UNSTABLE` due to `Client Tests` FAILURE. The most likely cause is that #332's branch hasn't been rebased onto post-#331 `main`, where the `ReleaseNotes` relative-time regex was already broadened. Once #334 lands on `main` (deterministic fixture fix) and #332 rebases, its Client Tests should go green without code changes to that PR. Spawning a review worker on #332 *now* risks duplicate effort (the worker would start writing its own fixture/regex fix in parallel with the merge worker's squash). Holding it for the next tick once `main` has the pinned fixtures is the cheaper path.

**Current state**

- Open PRs:
  - [PR #334](https://github.com/jpshackelford/voice-relay/pull/334) — `CLEAN/MERGEABLE`, merge worker dispatched.
  - [PR #332](https://github.com/jpshackelford/voice-relay/pull/332) — `UNSTABLE` (Client Tests failing on stale `main`). Deferred to next tick after #334 merges.
- Ready issues without an open PR: 0 (#297 → #332, #333 → #334).
- Issues needing expansion: 0.
- `on-hold` (S3 design freeze per AGENTS.md): #210, #239, #299, #300, #301, #302 — untouched.

**`quiet_ticks` = 0** (productive tick: 2 workers drained, 1 spawned).

_This worklog entry was generated by an AI agent (OpenHands /orchestrate) on behalf of @jpshackelford._

---
### 2026-05-25 01:36 UTC - Merge Worker (PR #334)

✅ **Merged: PR #334 - fix(tests): pin ReleaseNotes mock fixtures relative to Date.now()**

- PR: [#334](https://github.com/jpshackelford/voice-relay/pull/334) (squash-merged)
- Issue: [#333](https://github.com/jpshackelford/voice-relay/issues/333) — auto-closed via `Fixes #333` trailer
- Scope: test-only, single file (`client/src/components/ReleaseNotes.test.tsx`)

**Merge state at decision time:**
- `mergeStateStatus=CLEAN`, `mergeable=MERGEABLE`
- 9/9 required checks green (Client Tests, Server Tests, E2E, Build Client, lint-pr-title, enable-orchestrator, pr-review SUCCESS)
- 0 unresolved review threads; only `github-actions` had COMMENTED reviews

**Squash commit:** `fix(tests): pin ReleaseNotes mock fixtures relative to Date.now() (#334)` — scope `tests` is in the allowed vocabulary per AGENTS.md.

**Production impact:** None — test-only fix. No DB schema change, no migration. App auto-deploys to vr.chorecraft.net on merge to main; production behavior unchanged.

**Note:** PR #331 (merged earlier) had pre-emptively broadened the relative-time regex; #334 makes the fix deterministic by pinning the fixture itself instead of relying on a permissive regex. Both layers now in place on main.

_This worklog entry was generated by an AI agent (OpenHands merge worker) on behalf of @jpshackelford._

---
### 2026-05-25 01:49 UTC - Orchestrator (productive tick)

🚀 **Re-ran stale Client Tests on PR #332** so it can pick up the fixture pin from #334 on main.

**Drained from previous tick:**

| Conv ID | Type | Outcome |
|---------|------|---------|
| `bef3099` | review (merge) | Squash-merged [PR #334](https://github.com/jpshackelford/voice-relay/pull/334) (test fixture pin); issue #333 auto-closed |

**Active workers (after this tick):** none.

**Slot usage:** expansion 0/4 · implementation 0/1 · review 0/2

**Action taken**

- **Re-ran [run 26375625797](https://github.com/jpshackelford/voice-relay/actions/runs/26375625797) → `Client Tests (pull_request)`** on PR #332 via `gh run rerun --failed`. The job last ran at 2026-05-24 23:22 UTC against `main` *before* #334 fixture pin landed, and failed exactly on `ReleaseNotes.test.tsx > timestamp toggling > toggles back to relative time on second click` — i.e. the precise test the #334 fix unblocks. GitHub will re-merge `pull/332/merge` against current `main` for the re-run, so the deterministic `Date.now()`-pinned fixture will be in scope and the assertion should now pass without any code change to #332.

**Why this is an orchestrator-side action (not a worker spawn)**

Spawning a merge worker on `UNSTABLE` CI risks the worker forking its own redundant fix-PR (it does not know the failure is stale-against-main rather than a real bug in #332). A simple `rerun --failed` is one line, leaves no state for the worker to misinterpret, and lets the next tick spawn a merge worker against a clean CI. Cheaper and lower-blast-radius.

**Why no other workers spawned**

- No issues need expansion (all open issues are `ready` or `on-hold`).
- The only `ready` open issue is #297, which is already implemented by PR #332.
- All `on-hold` issues (S3 design freeze: #210, #239, #299, #300, #301, #302) are correctly untouched per AGENTS.md.
- Implementation slot would have nothing to pick up.

**Next tick expectations**

- PR #332 Client Tests re-run completes → `mergeStateStatus` flips `UNSTABLE → CLEAN` (all 7 checks green).
- Orchestrator spawns a single merge worker for PR #332 (`scope:server-only`, `priority:medium`, closes #297).
- After #332 merges, productive backlog reaches zero (only on-hold S3 freeze remains). Expect quiet ticks from there on; auto-disable counter will start incrementing.

**`quiet_ticks` = 0** (productive tick: drained 1 merge worker, kicked off CI rerun that unblocks the only open PR).

_This worklog entry was generated by an AI agent (OpenHands /orchestrate) on behalf of @jpshackelford._

---
### 2026-05-25 02:05 UTC - Orchestrator (productive tick)

🚀 **Updated PR #332 branch** to pull in #334's fixture pin from `main` — the previous tick's `gh run rerun --failed` was insufficient and the symptom recurred.

**Active workers (after this tick):** none.

**Slot usage:** expansion 0/4 · implementation 0/1 · review 0/2

**Drained from previous tick:** none new (last tick took a CI rerun, not a worker spawn).

**Diagnosis of why last tick's fix didn't stick**

Previous orchestrator (01:49 UTC) called `gh run rerun --failed` on run 26375625797, hoping the rerun would pick up #334's deterministic fixture from `main`. It did not — `gh run rerun` re-executes the **same commit SHA** the original run was tied to, which was the pre-#334 merge ref for `pull/332/merge`. So the rerun (completed 01:49:14Z) re-failed on the exact same assertion:

```
FAIL src/components/ReleaseNotes.test.tsx > timestamp toggling > toggles back to relative time on second click
AssertionError: expected 'last week' to match /ago|yesterday|today|just now/i
```

PR #332 itself only touches `server/src/agent-driver/{rebind,replay,openhands}*` — no client code, no client tests. The failure is a stale-against-main flake that #334 already fixed on main; #332 just needs main merged into its branch to see the fix.

**Action taken**

- `PUT /repos/jpshackelford/voice-relay/pulls/332/update-branch` — GitHub creates a merge commit from `main` (`f5bb075`) into `feat/issue-297-memory-replay-suffix` (`59fcbbc`). This produces a new HEAD SHA, which triggers fresh CI against the post-#334 tree. Client Tests should now go green with no code change to #332.
- Response: `{"message":"Updating pull request branch."}` — async, head OID will advance shortly.

**Why this is an orchestrator-side action (not a worker spawn)**

A single API call is cheaper and safer than spawning a review worker that would have to (a) diagnose the same staleness, (b) checkout the branch, (c) merge main locally, (d) push. The worker path also risks the bot writing a redundant code "fix" to ReleaseNotes.test.tsx, which would conflict with the #334 fixture that's already correct on main. Pushing a clean merge commit via the API gives the merge worker (next tick) a clean CI to inspect.

**Current state**

- Open PRs:
  - [PR #332](https://github.com/jpshackelford/voice-relay/pull/332) — `UNSTABLE` (Client Tests failing on stale merge SHA). Branch-update API call dispatched; fresh CI imminent. Server-only diff, `scope:server-only`, `priority:medium`, closes #297.
- Ready issues without an open PR: 0 (only #297, which is #332).
- Issues needing expansion: 0.
- `on-hold` (S3 design freeze per AGENTS.md): #210, #239, #299, #300, #301, #302 — untouched.

**Next tick expectations**

- Branch update completes → fresh CI run on new merge SHA → Client Tests pass → `mergeStateStatus` flips `UNSTABLE → CLEAN`.
- Spawn a single merge worker for PR #332 (`feat(server)` conventional commit, closes #297 via `Fixes #297` trailer).
- After #332 merges, productive backlog reaches zero. Quiet ticks resume; auto-disable counter starts incrementing.

**`quiet_ticks` = 0** (productive tick: API call unblocks CI on the only open PR).

_This worklog entry was generated by an AI agent (OpenHands /orchestrate) on behalf of @jpshackelford._

---
### 2026-05-25 02:22 UTC - Orchestrator

🚀 **Spawned: Merge Worker for PR #332**

**Active Workers:**
| Conv ID | Type | Working On | Status |
|---------|------|------------|--------|
| `5c170b5` | merge | [PR #332](https://github.com/jpshackelford/voice-relay/pull/332) — feat(server): memory replay after rebind via system_message_suffix | **NEW** |

**Merge Worker — PR #332**
- Closes: [#297](https://github.com/jpshackelford/voice-relay/issues/297) (Memory replay after rebind via condense + system_message_suffix)
- Scope: `scope:server-only` · `priority:medium` · `enhancement`
- CI: 5/5 SUCCESS (Server Tests, Client Tests, Build Client, lint-pr-title, plus deploy gate) · `MERGEABLE`/`CLEAN` · 0 unresolved review threads · not draft
- Worker is instructed to verify scope contract, recheck `on-hold`/`needs-human` labels right before merge, squash with `feat(server):` prefix, and let `Fixes #297` auto-close the issue.
- Conversation: [`5c170b5`](https://app.all-hands.dev/conversations/5c170b531dff47eaa8e50c9751a070e6)

**Current State (post-spawn):**
- Open PRs: 1 — PR #332 (now being merged)
- Ready issues: #297 (covered by PR #332)
- Needs expansion: 0 🎉
- On-hold (S3 design freeze + others): #210, #239, #299, #300, #301, #302
- Slots used: 0/4 expansion · 0/1 implementation · 1/2 review

**Housekeeping this tick:**
- Archived 12 entries from WORKLOG.md (older than the 6-hour productive window) to `WORKLOG_ARCHIVE_2026-05-24.md` — committed in db46107.

**Reasoning / no other workers spawned:**
- No issues need expansion → 0 expansion workers.
- The only `ready` issue (#297) is the one PR #332 closes → no implementation worker.
- 6 remaining open issues are all `on-hold` (5 on the S3 persistence design freeze pending `VR_WORKSPACE_BUCKET` provisioning per AGENTS.md, plus #210 and #239) → must not pick up.
- 1 PR open, 1 review slot used, 1 review slot free but no other PRs to act on.

**Quiet-tick counter:** 0 (productive tick).

---
### 2026-05-25 02:24 UTC - Merge Worker (PR #332)

✅ **Merged: PR #332 — feat(server): memory replay after rebind via system_message_suffix**

**Merge details**
- Squash-merged at 2026-05-25 02:23:40 UTC, merge SHA `7eafaea`.
- Base: `main` ← `feat/issue-297-memory-replay-suffix`.
- Conventional commit subject preserved (`feat(server):` prefix, satisfies `lint-pr-title`).
- Closes [#297](https://github.com/jpshackelford/voice-relay/issues/297) — auto-closed via `Fixes #297` at 02:23:41 UTC (state `COMPLETED`).

**Pre-merge gate audit**
| Gate | Result |
|---|---|
| Labels (`on-hold`/`needs-human` re-checked at merge) | clean — `enhancement`, `priority:medium`, `scope:server-only` |
| CI | 5/5 SUCCESS (Server Tests, Client Tests, Build Client, E2E, lint-pr-title) |
| Mergeability | `MERGEABLE` / `CLEAN` |
| Review threads | 0 unresolved (1 automated "Good taste" review only) |
| Scope contract (`scope:server-only`) | held — all 6 changed files under `server/**` |
| DB migration safety | n/a — no migration files touched; no schema risk on prod SQLite |

**What shipped (server-only)**
- `server/src/agent-driver/replay.ts` (new, 313 LOC): pure helpers for turning the platform-side event log into a `system_message_suffix` — `buildSuffixFromCondense`, `buildFallbackSuffix` (size-capped + recency-preserving, 2 KB cap, last 2 turns always intact), `buildReplaySuffix` wrapper, `CondenseFn` seam with `noopCondense` default.
- `server/src/agent-driver/rebind.ts`: `OpenHandsRebindClient` / `RebindOptions` accept optional `systemMessageSuffix`, forwarded on every retry attempt.
- `server/src/openhands.ts`: `OpenHandsClient.rebindConversation` serialises `system_message_suffix` on the `POST /app-conversations` body when non-empty; `AISessionManager.doRebindSession` pre-fetches the event log (`getEventsPage(limit=100)`), builds the replay suffix with the installed `condenseImpl`, and threads it through. Replay prep failure is non-fatal — rebind still proceeds.
- Tests: +345 LOC `replay.test.ts` (30 unit tests, T-4.2.U.*), +59 LOC `rebind.test.ts` (4 suffix-forwarding tests, T-4.2.I.1 + retry passthrough), +274 LOC `openhands.test.ts` (7 integration tests, T-4.2.I.1..I.6 + prep-failure-doesn't-block-rebind). Server suite: 1130 passing.

**Production impact (vr.chorecraft.net)**
- Auto-deploy will pick up `7eafaea` shortly. Server-only change; no client bundle change.
- No DB migrations → SQLite schema untouched.
- Behaviour change: after MISSING-sandbox rebind, the rebound agent now sees a memory-replay suffix derived from the prior event log via the fallback path (condense seam is a no-op in production until an offline condense impl is wired). Rebind itself is unchanged when replay prep fails.

**Backlog state (post-merge)**
- Open PRs: 0.
- Issues closed this tick: #297.
- Ready issues without an open PR: 0.
- `on-hold` (S3 design freeze + #210/#239): unchanged.

**`quiet_ticks` reset to 0** (productive tick: PR merged, issue closed).

_This worklog entry was generated by an AI agent (OpenHands /orchestrate merge worker) on behalf of @jpshackelford._

---
### 2026-05-25 02:48 UTC - Orchestrator

🔒 **Auto-disabled due to inactivity**

Two consecutive quiet ticks detected — no productive work available. Automation disabled to prevent unnecessary runs.

**State at disable**
- Open PRs: 0 (PR #332 squash-merged at 02:23:40 UTC → issue #297 closed)
- Ready issues without an open PR: 0
- Issues needing expansion: 0
- All 6 open issues are `on-hold`:
  - #298 (re-scoped Path B — *closed; this is a sibling reference*), #299, #300, #301, #302 → S3 persistence design freeze pending `VR_WORKSPACE_BUCKET` + AWS creds in `.env` on vr.chorecraft.net (see AGENTS.md "Active design freeze" + DEPLOYMENT.md)
  - #239 (flaky AI integration tests / OpenHands API reliability) → `on-hold` pending upstream stability
  - #210 (categorize deployment failures) → `on-hold`

**Quiet tick history**
| Tick | UTC | Why quiet |
|---|---|---|
| 1 | 2026-05-25 02:33 | PR #332 just merged; backlog all on-hold |
| 2 | 2026-05-25 02:48 | Same; no labels changed; freeze still active |

**Automation API confirmation**
```
PATCH /api/automation/v1/5f180989-ed9c-42b4-ac9f-5f30f0623316
→ { "id": "5f180989-ed9c-42b4-ac9f-5f30f0623316", "enabled": false, "name": "Voice Relay Workflow Orchestrator v2" }
```

**To re-enable**
- OpenHands UI: https://app.all-hands.dev/automations → "Voice Relay Workflow Orchestrator v2" → toggle on
- Or via API:
  ```bash
  curl -X PATCH "https://app.all-hands.dev/api/automation/v1/5f180989-ed9c-42b4-ac9f-5f30f0623316" \
    -H "Authorization: Bearer ${OPENHANDS_API_KEY}" \
    -H "Content-Type: application/json" \
    -d '{"enabled": true}'
  ```

**What will unblock the backlog**
- For #299–#302: complete S3 provisioning per the freeze-lift criteria in AGENTS.md (env vars set on production `.env`, bucket created, `/api/internal/health/s3` smoke passes). Then strip `on-hold` from #299–#302 and re-enable the automation.
- For #239: investigate upstream OpenHands API reliability; or rewrite the integration tests against a fake driver (now that `AgentDriver`/`FakeDriver` from #287 are merged).
- For #210: needs human prioritisation — has been `on-hold` without a freeze rationale; consider triage.

_This worklog entry was generated by an AI agent (OpenHands /orchestrate) on behalf of @jpshackelford._